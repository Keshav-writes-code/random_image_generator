{
  "extends": "astro/tsconfigs/strict",
  "exclude": ["src-tauri", "dist"],
  "compilerOptions": {
    "baseUrl": ".",          // Base is the root of the project
    "paths": {
      "@/*": ["src/*"]       // @ maps to the src/ directory
    }
  }
}
